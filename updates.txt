================================================================
UPDATES LOG
================================================================

----------------------------------------------------------------
2026-06-23 (charts + ML)
----------------------------------------------------------------

CREDITS BY USAGE TYPE / WEEK (new chart)
- New shared builder app/shared/chart_data.py -> usage_type_weekly():
  weekly credits split by usage type (top-N + "Other"), order-ranked.
- Rendered as a stacked bar with a Stacked/Grouped toggle on THREE pages:
  Summary, Forecast, and Tiers (on Tiers it respects the page filters).

REUSABLE CHART INFRASTRUCTURE (less repetition, more structure)
- New Jinja macro app/templates/macros/charts.html -> chart_card(): one
  place for the card + toolbar (export PNG, fullscreen, optional zoom,
  optional bar-mode toggle). Used by all the new charts.
- chart-card / chart-toolbar styles moved into global bnl.css (removed the
  duplicated block from forecast.html).
- charts.js gained renderUsageTypeChart() + setUsageTypeStacked/Mode() so
  any page renders the chart with one call. Charts remain readable via the
  existing fullscreen modal + PNG export.

SCIKIT-LEARN LINEAR REGRESSION FORECAST MODEL
- Added scikit-learn to requirements; new LinearRegressionModel in
  app/forecast/prediction.py (registered alongside Deterministic + Monte
  Carlo). Fits weekly burn vs. week index, projects the trend forward, and
  derives P10/P90 bands from residual spread.
- ForecastContext gained an optional chronological weekly_series; the
  model-data endpoint now supplies it.
- New "Linear Trend (ML)" overlay toggle on the burndown chart (shows slope
  + R2), with state restored on reload and refreshed on granularity change.

----------------------------------------------------------------
2026-06-23
----------------------------------------------------------------

SNAPSHOT HISTORY TABLE (no more horizontal scroll)
- Rebuilt the snapshot table with a fixed 8-column layout that always
  fits the page width (no horizontal scrollbar). Columns consolidated:
  select+color swatch, Snapshot (date + editable label stacked), Status
  (pacing + forecast pills stacked), Weekly Burn, Remaining, End Balance
  (with exhaustion date as a sub-note), MC Risk, and actions.
- Added a scoped .snap-table style for a cleaner, more compact look.

MONTE CARLO IN SNAPSHOT OVERLAYS
- Added a "Monte Carlo bands" toggle in the quick-select bar beneath the
  burndown chart. When on, each selected snapshot's P10-P90 confidence
  bands are overlaid (for snapshots that carry MC data).
- "Generate weekly" is now a dropdown with two choices: Fast (no Monte
  Carlo) and Include Monte Carlo (slower). MC mode regenerates existing
  weekly snapshots so they gain bands, using a capped run count (<=2000)
  to stay responsive across many weeks.

----------------------------------------------------------------
2026-06-22 (later)
----------------------------------------------------------------

MULTI-SHEET UPLOAD
- The Summary page uploader now accepts multiple files at once. All
  selected sheets are merged into the current data in one action; order
  does not matter (merge is commutative).
- Added a "Replace current data instead of merging" checkbox for when a
  fresh start is wanted rather than appending.
- Uploaded sheets are read in memory and merged; the combined result is
  written as a single canonical current_data.csv (stale current_data.*
  siblings are cleaned up).

UPLOAD LOG (Settings)
- Every data-sheet upload is now recorded (new public method
  record_upload() on IngestionPipeline).
- The Settings "Upload Log" table now shows, per upload: when, type
  (data sheet / historical / weekly), file, rows Added, total rows, and
  credits. A "replace" badge marks replace-mode uploads.

FORECAST SNAPSHOTS UI (decluttered)
- Added a quick-select dropdown directly beneath the burndown chart to
  overlay any past snapshots, with removable pills showing the current
  selection. This replaces the wide, horizontally scrolling table as the
  primary way to compare snapshots.
- The full snapshot table is now tucked into a collapsible
  "Manage & details" panel (collapsed by default), so the page no longer
  scrolls sideways or feels cluttered.
- Comparison cards still appear automatically when 2+ snapshots are
  selected (via the dropdown or the table).

TESTING
- testfiles/test_data_merge.py - merge unit/scenario tests (unchanged).
- testfiles/verify_upload_route.py - new integration test driving the
  multi-file /upload-data route through the Flask test client; backs up
  and restores live data so nothing is disturbed.
- Verified: all templates compile, app factory imports, and the
  multi-upload route + logging pass end-to-end.

----------------------------------------------------------------
2026-06-22
----------------------------------------------------------------

DATA APPENDING / MERGING (foolproof, order-independent)
- Added new shared module: app/shared/data_merge.py
    * normalize_date_partition() - rewrites date_partition to canonical
      ISO (YYYY-MM-DD). Each file is parsed while still single-format so
      mixed formats (e.g. M/D/YYYY vs YYYY-MM-DD) can no longer corrupt
      comparison or storage.
    * merge_usage_data(existing, new) - single entry point for merging an
      uploaded file into the current data. Safe when there is no existing
      data (fresh load).
    * dedupe_usage() - keeps one row per natural record key
      (date_partition, account_id, user id, usage_type). On a value
      conflict the larger usage_credits is kept. Because max is
      commutative, the merged result is identical regardless of upload
      order. Falls back to exact-row dedup if a safe key cannot be built.

- Refactored app/dashboard/routes.py (upload-data handler):
    * Now reads the file and delegates to merge_usage_data().
    * Removed the previous inline append/dedup logic.
    * Upload now reports how many genuinely new records were added vs a
      full-overlap no-op, and surfaces errors as a flash message instead
      of failing.

BEHAVIOR GUARANTEES
- Overlapping files: each record counted once, never double-counted.
- Conflicting values for the same day/user/usage_type: larger credits
  wins, deterministically.
- Different date formats across files: normalized before any comparison.
- Any upload order: identical final result.
- Bad/unexpected files: handled gracefully (flash message, no crash).
- Filenames are ignored for date logic; only the in-file date_partition
  values are used (export filenames did not reflect actual data ranges).

NOTE ON CONFLICT RULE
- Current rule is "largest credits wins" (assumes a fuller export
  supersedes a partial one). Switching to "most recently uploaded wins"
  is a one-line change in dedupe_usage(), but would reintroduce
  order-dependence, so it was intentionally not used.

EARLIER SAME-DAY WORK
- Settings page: added "Export Data" section to download the current
  data file; tier tabs no longer show a horizontal scrollbar (wrap
  instead).
- Forecast page: added "Generate weekly" button to create one snapshot
  per operational week automatically (derives weekly data from the daily
  store when no pipeline weekly data exists; skips Monte Carlo for speed
  and skips weeks already snapshotted). Snapshot rename changed from
  double-click to an explicit pencil icon.

TESTING / HOUSEKEEPING
- Consolidated all scratch test scripts into testfiles/:
    * test_data_merge.py - mock order-independence, conflict resolution,
      mixed date formats, and a real-file round-trip through DataStore.
    * diagnose_data.py - inspects a batch of files for date ranges,
      record-key uniqueness, and cross-file value conflicts.
- Removed the temporary scripts from the project root; root now contains
  only config.py and run.py.
- All test-file paths resolve relatively from the repo root (no absolute
  or user-specific paths hard-coded).
- Verified: full test suite passes and the Flask app factory imports
  cleanly.

================================================================
